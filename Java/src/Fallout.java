/**
 * Used to make sure the assigned points doesn't go over the maximum possible
 *
 * @author CentreMetre
 * @version 1.0
 * @since 0.0.0.1
 */

enum Fallout {
    FALLOUTTHREE,
    FALLOUTNEWVEGAS,
    FALLOUTFOUR
}